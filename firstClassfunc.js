/* Func that can be passed into another func as argument and can be return from a function or func can be assigned to any other variable */
const geek = (a,b) =>{
    return(a+" "+b);
}
console.log(geek("Fazal", "Karim"));